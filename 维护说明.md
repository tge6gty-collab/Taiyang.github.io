# 个人网站维护说明

> 葛泰扬 · AI 产品经理个人主页 · 穆夏 (Art Nouveau) 风格
> 线上地址：https://tge6gty.github.io
> GitHub 仓库：https://github.com/tge6gty/Taiyang.github.io

---

## 一、这个网站是怎么运作的（一分钟看懂）

- 纯静态网站：只有 HTML + CSS + JS + 图片，**没有后端、没有数据库、不需要任何构建工具**。
- 真正的"网站"活在 **GitHub 云端仓库** 和 **GitHub Pages 服务器**上，你的电脑只是一个"编辑副本"。
- 所有图片素材都已保存在仓库的 `assets/` 目录里（本地化存储），**不依赖任何外部网站**，所以外链失效、换电脑都不影响网站显示。

---

## 二、换电脑后如何继续维护（只需 3 步）

### 前提：新电脑装好 Git
- 下载安装 [Git](https://git-scm.com/downloads)
- 首次使用配置一次身份（用你的 GitHub 邮箱）：
  ```
  git config --global user.name "tge6gty"
  git config --global user.email "gty22333@163.com"
  ```

### 第 1 步：把网站克隆到新电脑
```
git clone https://github.com/tge6gty/Taiyang.github.io.git
```
克隆下来就是完整的、能直接双击 `index.html` 打开的网站，和旧电脑一模一样。

### 第 2 步：修改内容
用任意编辑器（推荐 VS Code）打开文件夹，改完后本地双击 `index.html` 预览效果。

### 第 3 步：发布上线
```
git add .
git commit -m "更新内容"
git push
```
推送后，GitHub Pages 会**自动重新部署**，约 1 分钟后线上网站即更新。

> 换电脑唯一要记住的两件事：① GitHub 账号登录（push 时会要求，用账号密码或 Personal Access Token）② 仓库地址（上面那个）。**不需要装任何环境、不需要配置、复杂度几乎为零。**

---

## 三、文件结构说明

```
个人网站/
├── index.html          # 页面结构（6 大板块）
├── style.css           # 全部样式（穆夏配色 + 排版 + 动效）
├── script.js           # 交互逻辑（打字机 / 灯箱 / 数字滚动等）
├── persoal_pic.jpeg    # 你的个人照片
├── assets/             # 素材目录（全部本地化，无外链依赖）
│   ├── mucha-zodiac.jpg    # 穆夏《黄道十二宫》— Hero 头像光环
│   ├── mucha-summer.jpg    # 穆夏《夏》— Hero 左侧背景
│   ├── mucha-winter.jpg    # 穆夏《冬》— Hero 右侧背景
│   ├── mucha-spring.jpg    # 穆夏《春》— 备用
│   ├── mucha-autumn.jpg    # 穆夏《秋》— 备用
│   ├── orn-divider.jpg     # 穆夏玫瑰卷须横幅 — 章节分隔条（真实素材）
│   ├── corner-tl.jpg       # 穆夏心形叶饰角 — 卡片/名片四角装饰（真实素材裁切）
│   ├── orn-roundel.jpg     # 穆夏卷须圆饰 — 实习卡片徽章印记（真实素材）
│   └── bg-pattern.svg      # 页面底纹（轻量背景纹理）
└── 维护说明.md          # 本文件
```

> 所有穆夏画作均为**公有领域**（作者 1939 年逝世，作品已进入 Public Domain，可自由使用），下载自 Wikimedia Commons。

---

## 四、常见修改指引（在 index.html 中）

| 想改什么 | 找哪里 |
|---------|--------|
| 首页标题轮播文案 | `script.js` 顶部的 `titles` 数组 |
| 个人简介 | `<section id="about">` 里的 `about-bio` |
| 教育背景 | `<section id="about">` 里的 `timeline` |
| 技能标签 | `<section id="skills">` 里的 `skill-accordion` |
| **新增实习经历** | `<section id="experience">` 里复制一个 `<article class="exp-card ...">` 修改 |
| 业绩数字（含滚动动画） | 经历卡片里的 `data-target` / `data-prefix` / `data-suffix` |
| 独立作品 | `<section id="projects">` 里的 `project-card` |
| 联系方式 | `<section id="contact">` 里的 `contact-card` |
| 配色 | `style.css` 顶部 `:root` 里的颜色变量 |

### 新增一段实习经历的示例
在 `#experience` 里复制一张卡片，修改这几处：
- `data-title` / `data-meta`：标题与时间地点
- `data-desc`：详情描述（点击卡片弹窗显示）
- `data-points`：详情要点，用 `|` 分隔多条
- `data-tags`：标签，用 `,` 分隔
- `exp-logo` 的文字与背景色
- `exp-metrics` 里的 `metric-num`（`data-target` 是滚动到的目标数字）

---

## 五、注意事项

- 图片命名、路径不要随意改动，否则页面会显示裂图。
- 修改后务必本地预览确认无误再 `git push`。
- `.reveal` 元素采用渐进增强：有 JS 时有入场动画，无 JS 时也能正常显示，对 SEO 和无障碍友好。
- 网站已适配手机端，改动后建议用手机也看一眼。
